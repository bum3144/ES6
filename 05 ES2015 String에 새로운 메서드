//ES2015 string에 새로운 메서드

let str = "hello world ! ~~!";
let matchstr = "~~!";
// startsWith, endsWith 시작과 끝이 일치하는 체크하는 방법
//console.log(str.startsWith(matchstr));
//console.log(str.endsWith(matchstr));

// includes 포함하는 글자가 있는지 체크
console.log("include test: ", str.includes("world"));
